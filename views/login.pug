extends layout

block content
  
  div(class="container")
    h1 Here is where the user should login
    form(action="/login" method="POST")
      input(type="text" name="username" placeholder="username")
      input(type="text" name="password" placeholder="password")
      button(type="submit") Log In
